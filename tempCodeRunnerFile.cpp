nt ans=0;
    // if (key >= rotated[pivot] && key <= rotated[n-1])
    // {
    //     ans = binarySearch(rotated, pivot,n-1, key);
    // }
    // else{
    //         ans = binarySearch(rotated, 0,pivot-1, key);
    // }